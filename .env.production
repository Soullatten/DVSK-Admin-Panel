# Backend API URL the packaged Electron app talks to.
# Default = your local backend during local testing.
# When you deploy your backend to a real server, change this to the public URL,
# e.g. VITE_API_URL=https://api.dvsk.com/api
VITE_API_URL=http://localhost:5000/api
